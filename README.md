# vivaldi-shim

This will be a macOS shim to make sure external links load in Vivaldi when there's no open window (for raindrop.io, CLI authentication, etc.)

The objective is an app bundle that can be set as the default browser.
